package com.sageburner.im.server.test.dao;

import com.sageburner.im.server.server.model.User;
import org.junit.Test;

/**
 * Created by Ryan on 10/17/2014.
 */
public class UserDaoTest {

    @Test
    public void addUserTest(User user) {
//        Session session = this.sessionFactory.getCurrentSession();
//        session.persist(user);
//
//        User userTest = (User) session.load(User.class, user.getUsername());
//
//        assertEquals(userTest.getUsername(), user.getUsername());
//        assertEquals(userTest.getUpdatedAt(), user.getUpdatedAt());
    }
}
